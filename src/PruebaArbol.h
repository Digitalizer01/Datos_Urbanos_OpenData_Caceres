/*
 * PruebaArbol.h
 *
 */

#ifndef PRUEBAARBOL_H_
#define PRUEBAARBOL_H_

class PruebaArbol {
public:
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada. constructor por defecto
	 *
	 * Complejidad: O(1)
	 */
	PruebaArbol();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada. prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetEspecie();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetFamilia();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetNombreComun();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetGenero();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetDiametro();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetAltura();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetCopa();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetRiego();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetUnidades();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */
	void pruebaGetySetCodigoVia();
	/*
	 * PRE:
	 * POST: prueba el método del mismo nombre de la clase deseada.
	 *
	 * Complejidad: O(1)
	 */

	void pruebaOperator();
	/*
	 * PRE:
	 * POST: ejectua las pruebas.
	 *
	 * Complejidad: O(1)
	 */

	void pruebaArbolrun();
	/*
	 * PRE:
	 * POST: destructor.
	 *
	 * Complejidad: O(1)
	 */
	~PruebaArbol();
};

#endif /* PRUEBAARBOL_H_ */
